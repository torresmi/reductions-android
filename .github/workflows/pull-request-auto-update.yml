# Auto updates pull request if target branch changes

name: Pull Request Auto Update

on:
  push: {}

jobs:
  auto-update:
   name: auto-update
   runs-on: ubuntu-latest
   steps:
    - uses: docker://chinthakagodawita/autoupdate-action:v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
