import dependencies.Deps

apply plugin: 'kotlin'

sourceCompatibility = JavaVersion.VERSION_1_8
targetCompatibility = JavaVersion.VERSION_1_8

test {
    useJUnitPlatform()
}

dependencies {
    compile project(':domain')
    compile project(':core')

    compile Deps.Arrow.core
    compile Deps.Arrow.syntax
    compile Deps.Coroutines.core
    compile Deps.kotlin
    compile Deps.moshi
    compile Deps.Retrofit.moshiConverter
    compile Deps.remoteData
    compile Deps.Retrofit.core

    testCompile Deps.Coroutines.test
    testCompile Deps.kotlinTest
    testCompile Deps.mockk
    testCompile project(':test-util')
}
