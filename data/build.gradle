apply plugin: 'kotlin'
apply plugin: 'com.github.ben-manes.versions'

sourceCompatibility = "1.7"
targetCompatibility = "1.7"

dependencies {
    compile project(':core')

    compile kotlinStdLib
    compile moshi
    compile moshiConverter
    compile retrofit
}
