import dependencies.Deps

apply plugin: 'kotlin'

sourceCompatibility = JavaVersion.VERSION_1_8
targetCompatibility = JavaVersion.VERSION_1_8

test {
    useJUnitPlatform()
}

dependencies {

    compile project(':core')
    compile Deps.kotlinTest
    compile Deps.remoteData
}
