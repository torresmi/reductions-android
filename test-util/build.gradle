apply plugin: 'kotlin'

sourceCompatibility = JavaVersion.VERSION_1_8
targetCompatibility = JavaVersion.VERSION_1_8

test {
    useJUnitPlatform()
}

dependencies {

    compile project(':core')
    compile Libs.kotlintest_runner_junit5
    compile Libs.remotedata
}
